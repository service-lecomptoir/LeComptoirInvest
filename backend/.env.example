ENV=production
DEBUG=false
DATABASE_URL=postgresql+asyncpg://invest_user:CHANGE_ME@localhost:5432/lecomptoirinvest
# REQUIRED, no default: it signs the tokens AND derives the key that encrypts the
# investors' IBANs. Generate one per deployment and never reuse another product's.
SECRET_KEY=
# The fund's own accounts, one per currency it holds, comma separated.
FUND_IBANS=
ALICE_URL=
ALICE_INTERNAL_KEY=
